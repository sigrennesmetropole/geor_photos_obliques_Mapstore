import React from "react";
import Message from "@mapstore/components/I18N/Message";

const Select = (props) => {
    props = props.props;
    // console.log(props);
    return (
        <p>SelectComponent</p>
    );
};

export default Select;